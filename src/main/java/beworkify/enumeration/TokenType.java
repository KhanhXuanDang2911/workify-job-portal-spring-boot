package beworkify.enumeration;

/**
 * Enumeration for token types used in authentication.
 *
 * @author KhanhDX
 * @since 1.0.0
 */
public enum TokenType {
  ACCESS_TOKEN,
  REFRESH_TOKEN,
  RESET_TOKEN,
  CONFIRM_TOKEN,
  CREATE_PASSWORD_TOKEN
}
